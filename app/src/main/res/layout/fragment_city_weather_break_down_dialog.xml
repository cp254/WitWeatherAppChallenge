<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/list"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipToPadding="false"
    android:paddingTop="@dimen/list_item_spacing_half"
    android:paddingBottom="32dp"
    tools:context=".ui.main.CityWeatherBreakDownDialogFragment"
    tools:listitem="@layout/fragment_city_weather_break_down_dialog_list_dialog_item">

    <LinearLayout
        android:layout_width="match_parent"
        android:orientation="vertical"
        android:layout_height="match_parent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="The weather in Paris si"
            android:id="@+id/cityName"
            android:layout_margin="24dp"
            android:textSize="22sp"
            android:fontFamily="@font/montserratextrabold"/>

        <include
            layout="@layout/list_item_city_weather"/>


    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>